apiVersion: batch/v1
kind: CronJob
metadata:
  name: failure-classification
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: failure-classification-sa
          containers:
          - name: failure-check
            image: ppankajs/self-healing-app:latest
            command: ["python", "/app/scripts/failure_classification.py"]
            volumeMounts:
              - name: shared-data
                mountPath: /data
          restartPolicy: OnFailure
          volumes:
            - name: shared-data
              persistentVolumeClaim:
                claimName: score-storage
